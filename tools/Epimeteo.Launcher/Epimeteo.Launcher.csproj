<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>Epimeteo.Launcher</RootNamespace>
    <EpimeteoStrict>true</EpimeteoStrict>
  </PropertyGroup>

  <ItemGroup>
    <!-- Reusa el tipo Manifest de Epimeteo.ReleaseTool en vez de duplicarlo: mismo criterio que
         Epimeteo.ContentValidator referenciando Epimeteo.Server. -->
    <ProjectReference Include="..\Epimeteo.ReleaseTool\Epimeteo.ReleaseTool.csproj" />
  </ItemGroup>

</Project>
