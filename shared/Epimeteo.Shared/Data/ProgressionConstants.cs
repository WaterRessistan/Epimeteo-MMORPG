namespace Epimeteo.Shared.Data;

/// <summary>Números de progresión que tienen que valer lo mismo en cliente y servidor (FASE-10).</summary>
public static class ProgressionConstants
{
    /// <summary>Puntos de stat que concede cada nivel.</summary>
    public const int StatPointsPerLevel = 3;
}
